unit Jury_rig;
interface
uses general,video;
procedure Rig_Bit2;
implementation
const suki=16;
      sake=128;
procedure Rig_Bit2;
begin
putimg24( 10,0,log1,vaddr);
putimg24( 35,0,log2,vaddr);
putimg24( 60,0,log3,vaddr);
putimg24( 85,0,log4,vaddr);
putimg24(110,0,log5,vaddr);
putimg24(135,0,log6,vaddr);
putimg24(160,0,log1,vaddr);
putimg24(185,0,log7,vaddr);
putimg24(210,0,log7,vaddr);
putimg24(235,0,log8,vaddr);
putimg24(260,0,log4,vaddr);
putimg24(285,0,log2,vaddr);
putimg16(suki+ 40, 64,met1,vaddr);
putimg16(suki+ 40, 88,wrl1,vaddr);
putimg16(suki+ 40,112,orb1,vaddr);
putimg8(suki+ 64, 64,let_c,vaddr);
putimg8(suki+ 72, 64,let_y,vaddr);
putimg8(suki+ 80, 64,let_m,vaddr);
putimg8(suki+ 88, 64,let_b,vaddr);
putimg8(suki+ 96, 64,let_a,vaddr);
putimg8(suki+104, 64,let_l,vaddr);
putimg8(suki+112, 64,let_o,vaddr);
putimg8(suki+120, 64,let_i,vaddr);
putimg8(suki+128, 64,let_d,vaddr);
putimg8(suki+ 64, 88,let_s,vaddr);
putimg8(suki+ 72, 88,let_p,vaddr);
putimg8(suki+ 80, 88,let_a,vaddr);
putimg8(suki+ 88, 88,let_c,vaddr);
putimg8(suki+ 96, 88,let_e,vaddr);
putimg8(suki+104, 88,let_w,vaddr);
putimg8(suki+112, 88,let_o,vaddr);
putimg8(suki+120, 88,let_r,vaddr);
putimg8(suki+128, 88,let_m,vaddr);
putimg8(suki+ 64,112,let_o,vaddr);
putimg8(suki+ 72,112,let_r,vaddr);
putimg8(suki+ 80,112,let_b,vaddr);
putimg8(suki+ 88,112,let_i,vaddr);
putimg8(suki+ 96,112,let_t,vaddr);
putimg8(suki+104,112,let_r,vaddr);
putimg8(suki+112,112,let_o,vaddr);
putimg8(suki+120,112,let_i,vaddr);
putimg8(suki+128,112,let_d,vaddr);
putimg8(suki+ 64, 72,let_pt,vaddr);
putimg8(suki+ 72, 72,let_pt,vaddr);
putimg8(suki+ 80, 72,let_pt,vaddr);
putimg8(suki+ 88, 72,let_pt,vaddr);
putimg8(suki+ 96, 72,let_pt,vaddr);
putimg8(suki+104, 72,let_pt,vaddr);
putimg8(suki+112, 72,let_1,vaddr);
putimg8(suki+120, 72,let_0,vaddr);
putimg8(suki+128, 72,let_0,vaddr);
putimg8(suki+ 64, 96,let_pt,vaddr);
putimg8(suki+ 72, 96,let_pt,vaddr);
putimg8(suki+ 80, 96,let_pt,vaddr);
putimg8(suki+ 88, 96,let_pt,vaddr);
putimg8(suki+ 96, 96,let_pt,vaddr);
putimg8(suki+104, 96,let_pt,vaddr);
putimg8(suki+112, 96,let_2,vaddr);
putimg8(suki+120, 96,let_5,vaddr);
putimg8(suki+128, 96,let_0,vaddr);
putimg8(suki+ 64,120,let_pt,vaddr);
putimg8(suki+ 72,120,let_pt,vaddr);
putimg8(suki+ 80,120,let_pt,vaddr);
putimg8(suki+ 88,120,let_pt,vaddr);
putimg8(suki+ 96,120,let_pt,vaddr);
putimg8(suki+104,120,let_pt,vaddr);
putimg8(suki+112,120,let_1,vaddr);
putimg8(suki+120,120,let_0,vaddr);
putimg8(suki+128,120,let_0,vaddr);
putimg16(sake+ 40, 64,sli1,vaddr);
putimg16(sake+ 40, 88,ooz1,vaddr);
putimg16(sake+ 40,112,han2,vaddr);
putimg8(sake+ 64, 64,let_r,vaddr);
putimg8(sake+ 72, 64,let_o,vaddr);
putimg8(sake+ 80, 64,let_t,vaddr);
putimg8(sake+ 88, 64,let_a,vaddr);
putimg8(sake+ 96, 64,let_b,vaddr);
putimg8(sake+104, 64,let_l,vaddr);
putimg8(sake+112, 64,let_a,vaddr);
putimg8(sake+120, 64,let_d,vaddr);
putimg8(sake+128, 64,let_e,vaddr);
putimg8(sake+ 64, 88,let_s,vaddr);
putimg8(sake+ 72, 88,let_l,vaddr);
putimg8(sake+ 80, 88,let_i,vaddr);
putimg8(sake+ 88, 88,let_m,vaddr);
putimg8(sake+ 96, 88,let_e,vaddr);
putimg8(sake+104, 88,let_b,vaddr);
putimg8(sake+112, 88,let_l,vaddr);
putimg8(sake+120, 88,let_o,vaddr);
putimg8(sake+128, 88,let_b,vaddr);
putimg8(sake+ 64,112,let_m,vaddr);
putimg8(sake+ 72,112,let_i,vaddr);
putimg8(sake+ 80,112,let_n,vaddr);
putimg8(sake+ 88,112,let_e,vaddr);
putimg8(sake+ 96,112,let_r,vaddr);
putimg8(sake+104,112,let_m,vaddr);
putimg8(sake+112,112,let_e,vaddr);
putimg8(sake+120,112,let_c,vaddr);
putimg8(sake+128,112,let_h,vaddr);
putimg8(sake+ 64, 72,let_pt,vaddr);
putimg8(sake+ 72, 72,let_pt,vaddr);
putimg8(sake+ 80, 72,let_pt,vaddr);
putimg8(sake+ 88, 72,let_pt,vaddr);
putimg8(sake+ 96, 72,let_pt,vaddr);
putimg8(sake+104, 72,let_pt,vaddr);
putimg8(sake+112, 72,let_5,vaddr);
putimg8(sake+120, 72,let_0,vaddr);
putimg8(sake+128, 72,let_0,vaddr);
putimg8(sake+ 64, 96,let_pt,vaddr);
putimg8(sake+ 72, 96,let_pt,vaddr);
putimg8(sake+ 80, 96,let_pt,vaddr);
putimg8(sake+ 88, 96,let_pt,vaddr);
putimg8(sake+ 96, 96,let_pt,vaddr);
putimg8(sake+104, 96,let_pt,vaddr);
putimg8(sake+112, 96,let_5,vaddr);
putimg8(sake+120, 96,let_0,vaddr);
putimg8(sake+128, 96,let_0,vaddr);
putimg8(sake+ 64,120,let_pt,vaddr);
putimg8(sake+ 72,120,let_pt,vaddr);
putimg8(sake+ 80,120,let_pt,vaddr);
putimg8(sake+ 88,120,let_pt,vaddr);
putimg8(sake+ 96,120,let_pt,vaddr);
putimg8(sake+104,120,let_1,vaddr);
putimg8(sake+112,120,let_0,vaddr);
putimg8(sake+120,120,let_0,vaddr);
putimg8(sake+128,120,let_0,vaddr);
putimg8( 80,168,let_h,vaddr);
putimg8( 88,168,let_i,vaddr);
putimg8( 96,168,let_g,vaddr);
putimg8(104,168,let_h,vaddr);
putimg8(120,168,let_s,vaddr);
putimg8(128,168,let_c,vaddr);
putimg8(136,168,let_o,vaddr);
putimg8(144,168,let_r,vaddr);
putimg8(152,168,let_e,vaddr);
putimg8(160,168,let_dp,vaddr);
putimg8( 64,176,let_p,vaddr);
putimg8( 72,176,let_r,vaddr);
putimg8( 80,176,let_e,vaddr);
putimg8( 88,176,let_s,vaddr);
putimg8( 96,176,let_s,vaddr);
putimg8(112,176,let_f,vaddr);
putimg8(120,176,let_i,vaddr);
putimg8(128,176,let_r,vaddr);
putimg8(136,176,let_e,vaddr);
putimg8(152,176,let_t,vaddr);
putimg8(160,176,let_o,vaddr);
putimg8(176,176,let_s,vaddr);
putimg8(184,176,let_t,vaddr);
putimg8(192,176,let_a,vaddr);
putimg8(200,176,let_r,vaddr);
putimg8(208,176,let_t,vaddr);
putimg8(224,176,let_g,vaddr);
putimg8(232,176,let_a,vaddr);
putimg8(240,176,let_m,vaddr);
putimg8(248,176,let_e,vaddr);
putimg8(  0,192,let_cp,vaddr);
putimg8( 16,192,let_sc,vaddr);
putimg8( 24,192,let_9,vaddr);
putimg8( 32,192,let_9,vaddr);
putimg8( 48,192,let_p,vaddr);
putimg8( 56,192,let_c,vaddr);
putimg8( 72,192,let_v,vaddr);
putimg8( 80,192,let_e,vaddr);
putimg8( 88,192,let_r,vaddr);
putimg8( 96,192,let_s,vaddr);
putimg8(104,192,let_i,vaddr);
putimg8(112,192,let_o,vaddr);
putimg8(120,192,let_n,vaddr);
putimg8(136,192,let_c,vaddr);
putimg8(144,192,let_o,vaddr);
putimg8(152,192,let_d,vaddr);
putimg8(160,192,let_e,vaddr);
putimg8(168,192,let_am,vaddr);
putimg8(176,192,let_g,vaddr);
putimg8(184,192,let_f,vaddr);
putimg8(192,192,let_x,vaddr);
putimg8(208,192,let_b,vaddr);
putimg8(216,192,let_y,vaddr);
putimg8(232,192,let_d,vaddr);
putimg8(240,192,let_pt,vaddr);
putimg8(248,192,let_c,vaddr);
putimg8(256,192,let_pt,vaddr);
putimg8(264,192,let_m,vaddr);
putimg8(272,192,let_pt,vaddr);
putimg8(288,192,let_s,vaddr);
putimg8(296,192,let_o,vaddr);
putimg8(304,192,let_f,vaddr);
putimg8(312,192,let_t,vaddr);
end;
begin
end.